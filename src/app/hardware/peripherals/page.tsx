"use client";

/**
 * @module HardwarePeripheralsPage
 * @description Route-level passthrough. Implementation at
 * src/components/command/nodes/ground-station/PeripheralsTab.tsx.
 * @license GPL-3.0-only
 */

import { PeripheralsTab } from "@/components/command/nodes/ground-station/PeripheralsTab";

export default function HardwarePeripheralsPage() {
  return <PeripheralsTab />;
}
